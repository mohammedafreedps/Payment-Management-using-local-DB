import 'package:flutter/material.dart';

class ScanReciptScreen extends StatelessWidget {
  const ScanReciptScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}